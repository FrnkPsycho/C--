#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#define ll long long
#define MAX 100

int h[MAX];

int main()
{
    int n;
    int cnt = 0;
    while (scanf("%d", &n) != EOF)
    {
        h[cnt++] = n;
    }
    int now=0;
    for ( int  )
    return 0;
}