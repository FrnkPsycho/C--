#include <iostream>
#include <string>
#include <cctype>
using namespace std;

int main()
{
    string line;
    int a,n,s,o;
    getline(cin,line);
    for ( int i=0 ; i<line.length() ; i++ )
    {
        cout << line[i] << " ";
    }
    if ( isalpha() ) 
    else if
    else if
    else if
    return 0;
}